class Transaction {

    list(financial_institution_id, account_id, customer_access_token, query_params) {
        throw('todo')
    }

    find(id, financial_institution_id, account_id, customer_access_token) {
        throw('todo')
    }

}

module.exports = Transaction;